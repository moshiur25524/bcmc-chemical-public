import React from 'react';
import { useParams } from 'react-router-dom';

const Purchase = () => {
    // const {id} = useParams();

    return (
        <div>
            <h1>This is perchase page  </h1>
            <p>And  it is protected</p>
        </div>
    );
};

export default Purchase;